Here are different types of loops in R with examples:

For Loop
```
Create a vector
vec <- c(1, 2, 3, 4, 5)

Use for loop to print each element
for (i in vec) {
  print(i)
}
```

While Loop
```
Initialize a variable
i <- 1

Use while loop to print numbers from 1 to 5
while (i <= 5) {
  print(i)
  i <- i + 1
}
```

Repeat Loop
```
Initialize a variable
i <- 1

Use repeat loop to print numbers from 1 to 5
repeat {
  print(i)
  i <- i + 1
  if (i > 5) {
    break
  }
}
```

Nested Loops
```
Use nested loops to print a matrix
for (i in 1:3) {
  for (j in 1:3) {
    print(paste("Row:", i, "Column:", j))
  }
}
```

Looping through Data Structures
```
Create a list
list <- list("a", 1, TRUE, c(1, 2, 3))

Use loop to print each element
for (i in list) {
  print(i)
}
```
