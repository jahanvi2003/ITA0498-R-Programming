num_samples <- 1000
random_numbers <- rnorm(num_samples, mean = 0, sd = 1)

random_numbers <- round(random_numbers, digits = 2)

occurrence_counts <- table(random_numbers)
print("Occurrences of each value:")
print(occurrence_counts)
